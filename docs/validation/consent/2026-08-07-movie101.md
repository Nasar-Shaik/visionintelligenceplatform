# Consent and provenance record — `movie101` (real footage)

> ⛔ **This record is INCOMPLETE and is not yet a lawful-basis record.** The sections marked
> **PENDING** can only be completed by the Architect, who was present at the recording. Until they
> are, this clip may be **ingested and annotated** but its results must not be published, exported,
> or presented outside this repository. See §5.

---

## 1. What is established from the file itself

Everything in this section was read from the bytes and is reproducible by re-running the commands in
`REAL_FOOTAGE_INGEST.md`. ⭐ Nothing here is attested; it is measured.

| | |
| --- | --- |
| Case id | `movie101` |
| File | `.data/real/movie101.mp4` (git-ignored; **the clip is not committed**) |
| sha256 | `e6f1448f54821fae4d5a197e464a08fc6c5a85453309f98fffc407aa99396107` |
| Size | 22 753 372 bytes |
| Resolution | 1080 × 1920 (portrait) |
| Source frame rate | 27.001 fps |
| Duration | 19.037 s (514 frames) |
| Container | QuickTime (`ftyp qt`), video + audio track |
| `mvhd` created | 2026-08-07T18:27:00Z |
| `com.apple.quicktime.creationdate` | 2026-08-07T23:56:31+05:30 (the same instant) |

## 2. ⚠️ Capture device — asserted by metadata, NOT established

The container declares:

| Key | Value |
| --- | --- |
| `com.apple.quicktime.make` | `Apple` |
| `com.apple.quicktime.model` | `MacBookPro18,1` |
| `com.apple.quicktime.software` | `macOS 26.5.2 (25F84)` |
| audio input | `MacBook Pro Microphone (Digital Mic)` |

⛔ **These do not establish the capture device**, and the record must not be read as if they did.
Two facts contradict the simple reading:

- Every frame carries a burned-in **`camphotoapp.com` watermark**, so the pixels passed through a
  third-party capture or export application. Metadata written by such an app describes the machine it
  ran on or exported from — not necessarily the sensor.
- The frame geometry is **portrait 1080 × 1920**. The built-in camera of a MacBookPro18,1 is a
  landscape sensor, so the stated model does not by itself account for the geometry.

⭐ The honest statement is: *a 27 fps portrait clip, produced with the `camphotoapp.com` application,
on or exported by an Apple machine on 2026-08-07.* Mounting, height and angle were **not recorded**
and cannot be recovered from the file. This matters downstream — `top-down-view` and `low-angle-view`
are declarable scenarios, and neither may be claimed for this clip without that measurement.

## 3. What the footage contains

One adult male in a domestic room; a clothes rail against a pale wall. The subject enters, stands,
turns, and leaves frame **three times**, with the room empty in between. No second person, no object
handling, no sitting, bending, or raised hands. Full scenario assessment is in the corpus entry and
in §4 of `CORPUS_COVERAGE.md`.

⚠️ The clip carries an **audio track**, which the perception pipeline never reads. If any voice on it
is identifiable, that is a separate disclosure the sections below must cover.

## 4. Relationship to earlier runs

⛔ **These are not new pixels.** This digest is byte-identical to `.soak-real.mp4`, the clip uploaded
through the deployed platform during the 2026-08-08 and 2026-08-09 soaks. Registering it now declares
provenance for footage the platform has **already processed** as an undeclared local file.

⭐ That is a strengthening, not a problem — the soak observations describe exactly these pixels — but
it must be stated, because it means the consent below is being recorded *after* the processing it
authorises rather than before.

## 5. ⛔ PENDING — the record an auditor would need, and does not yet have

The Architect stated on **2026-08-11**, in the instruction that opened this ingestion, that this is
*"the consented movie101.mp4 recording"*. That statement is recorded here faithfully and is the sole
basis on which registration proceeded.

⚠️ **It is an attestation, not a record.** It was not witnessed by the agent that wrote this file, and
it does not answer any of the following. Each line must be completed and the document signed before
this clip's results leave the repository:

- [ ] **Subject identity** — who appears in the footage, and their relationship to the Architect.
- [ ] **Consent given by the subject**, in their own words or signature, covering: recording;
      automated analysis; retention; and the specific fact that frames are extracted and stored.
- [ ] **Was the subject informed** the footage would be used to evaluate a detection system?
- [ ] **Retention and deletion** — how long `.data/real/movie101.mp4` and its 37 extracted frames are
      kept, and who deletes them.
- [ ] **Permitted use** — internal evaluation only, or may derived figures be published? ⛔ Default
      until answered: **internal only**.
- [ ] **Third-party terms** — whether the `camphotoapp.com` application's terms permit this use.
- [ ] **Audio** — whether the audio track carries identifiable speech, and whether it may be retained.
- [ ] **Withdrawal** — how the subject withdraws consent, and what is deleted when they do.

**Signed:** ______________________  **Date:** ____________

---

## 6. What was done under this record

| Step | Result |
| --- | --- |
| Registered in `benchmarks/detector-corpus.json` as `REAL_FOOTAGE` | 2026-08-11 |
| Digest verified against the declared bytes | ✓ |
| 37 frames extracted to `.data/real/movie101-frames/` (git-ignored) | ✓ |
| Annotation skeleton generated, unfilled | ✓ |
| Human annotation | ⏳ not started |
| Detector accuracy | ⛔ **not computed** — requires human annotation first |

⛔ No accuracy, precision or recall figure has been derived from this clip, and none may be until
Tier-1 annotations exist and pass `--validate-annotations`.

# Daily Development Log

> One rolling file, newest first. **Capture meaningful progress only — no exhaustive file lists** (those live in git history and per-slice [REVIEW_HISTORY](REVIEW_HISTORY.md)). One short entry per working day. Sign entries `[name · YYYY-MM-DD]`.

## 2026-07-29

- **P2-1.2 — Operations Console Authentication (code complete, ⏳ awaiting review).** Full auth vertical wired to the real identity API through the gateway (`/api/identity/auth/login|refresh|logout|me`). Design: an **`authClient`** is the single owner of session mutation — login persists the **rotating refresh token** (localStorage) while the **access token stays in memory only** (XSS-safe), then hydrates the principal via `/auth/me` and **expands the principal's roles into permission patterns using `@vip/permissions`** (the same wildcard-aware PDP the backend uses — `/auth/me` returns roles authoritatively, permissions empty). **Silent refresh** is a **single-flight** `refreshSession()`; the `http` client gained a 401-seam that calls it once and retries the original request, so an expired access token self-heals without bouncing the user to login. **`AuthBootstrap`** runs the one-time silent-refresh-on-load behind a splash (no login flash for returning users); **`RequireAuth`** guards the authenticated area; **`LoginPage`** uses React Hook Form + Zod with inline validation + API-error surfacing. `usePermission` now delegates to `can()` (wildcard-aware) for **deny-by-default** nav/action gating (the gateway stays the real authz boundary). Added `@vip/permissions` as a shared workspace dep (verified **browser-safe**, honouring app→shared). Removed the P2-1.0 FoundationReady splash (superseded by the auth landing). **Debugging notes** (both fixed, good catches): (1) `@hookform/resolvers@3.10` predates **Zod 4** (the repo's pinned zod) and threw the ZodError instead of populating RHF errors → bumped to **v5** (standard-schema based, Zod-4 native); (2) a `RequireAuth` unit test rendered the guard **without a `<Routes>`**, so its `<Navigate>` re-navigated in an infinite loop (OOM) — a test artifact, not an app bug (in-app it sits inside `<Routes>` and unmounts on redirect); rewrote the test with a real routes tree. **All gates green:** typecheck, lint, **18 tests** (6 new auth: authClient login/hydrate/role-expansion, bootstrap, login validation + submit via MSW, guard redirect+outlet, permission gating), build, import-graph 0-viol (18 pkgs), Prettier. No frozen doc / contract / backend service touched. Next: **P2-1.3 App Shell & Navigation**. `[Claude · 2026-07-29]`
- **P2-1.1 — Operations Console Design System (code complete, ⏳ awaiting review).** Built the full SOC design system on the P2-1.0 token layer, with the **Tailwind MCP** as the design assistant (session restarted to surface its tools). Opened with a rigorous **WCAG AA contrast pass** computed over the _actual_ oklch tokens (OKLCh→linear-sRGB→relative-luminance→ratio, not eyeballed): body text 15–17:1, every severity/status text ≥ AA — and it **caught a real gap**: the primary button (white-on-`--color-brand`) was 3.43:1. Fixed by giving the primary _button_ a dedicated `--color-primary` at L 0.55 (**4.59:1 AA**) while keeping `--color-brand` (0.62) as the accent for text/borders/rings (4.87:1 on surface). Built **19 themed shadcn/ui primitives** (Button/Card/Badge/Alert/Input/Textarea/Label/Select/Switch/Tabs/Dialog/Sheet-drawer/DropdownMenu/Tooltip/Skeleton/Separator/ScrollArea/Table[density-aware]/Toast) — Radix accessibility under the hood, cva variants → tokens (never raw hex), `data-state` enter/exit via CSS keyframes (no animation lib; `prefers-reduced-motion` honoured). Built **13 SOC composites** — StatusIndicator, SeverityBadge (1:1 with contract `EventPriority`), MetricCard (Recharts sparkline), CameraTile, VideoPlayerContainer, DetectionOverlay (normalized [0,1]→%), IncidentCard (severity left-border + status chip + quick actions), NotificationCard, Timeline, EmptyState, LoadingSkeletons, PageHeader, FilterBar — with severity/status rendered via **static class maps** (so Tailwind's compiler emits them; dynamic `text-${token}` would be purged) and accessible names (never colour-only, §8). **MCP-guided consolidation:** the repeated 5-class focus-visible chain became one `@utility focus-ring` (single source for keyboard focus). Shipped the **`/design` gallery** route (dev reference) rendering every primitive + variant + all severity/status states — also a mounts-everything smoke test. **All gates green:** typecheck, lint (incl. the no-hex/px rule, which caught + I fixed a stray `left-[3px]`), **13 tests** (Button/Badge/SeverityBadge/StatusIndicator-a11y/IncidentCard/Dialog-interaction + gallery smoke; added ResizeObserver/matchMedia polyfills for Recharts/jsdom), **build** (Tailwind → 37 kB CSS / gz 7.3 kB), import-graph 0-viol, Prettier. Deps added under `apps/console` only (10 Radix packages + sonner + recharts). No frozen doc / contract / backend service touched. Next: **P2-1.2 Authentication**. `[Claude · 2026-07-29]`
- **P2-1.0 — Operations Console Foundation (code complete, ⏳ awaiting review).** First frontend slice. Scaffolded **`apps/console`** on the frozen stack and added the **`apps/*` workspace glob** + a new **`app` import-graph layer** ([ADR-0019](../adr/ADR-0019-operations-console-and-apps-layer.md)): two rules — **`noAppToBackend`** (an app imports shared `packages/*` only — `@vip/contracts` for types — never services/plugins) and **`noImportApp`** (nothing imports an app; apps are graph leaves) — wired into `check-imports.mjs` and **negative-tested** (a probe import of `@vip/service-gateway` is correctly rejected, then removed → 0 violations, 18 pkgs). **Design-system-first:** authored `src/app/styles/theme.css` as the single Tailwind v4 `@theme` token block transcribed from the frozen [DESIGN_SYSTEM](../architecture/phase2/DESIGN_SYSTEM.md) (dark graphite oklch ramp, brand/semantic, 5-step severity ⇄ `EventPriority`, status colours, type/radius/motion scales, widescreen 3xl/4xl/5xl breakpoints, shadcn semantic aliases). Wired the app infra: **RTK store** (session/ui/live/filters slices + listener-middleware seam, `makeStore()` for test isolation, typed hooks + `usePermission` deny-by-default), **TanStack Query** client (server-state owner; 4xx→no-retry), **React Router** data router, a **typed gateway API client** (`/api/*`, envelope-unwrapping, in-memory bearer token — no localStorage, `ApiRequestError` carrying code/status/correlationId), and `severity`/`format`/`queryKeys`/`cn` libs. State ownership split enforced by design (Query = server, RTK = client, URL = view — §11). Test harness: Vitest + RTL + **MSW/node** + `renderWithProviders`; **5 tests green** (foundation splash mounts through the full provider stack; lib units). **All gates green:** typecheck, lint (incl. no-hex/px-in-className rule), **build** (Vite 6 → 9.7 kB CSS / 124 kB gz JS), import-graph 0-viol, repo Prettier. No frozen doc, contract, or backend service touched. Governance: [REVIEW_HISTORY](REVIEW_HISTORY.md) P2-1.0 row (⏳ pending — never self-approved), [MASTER_PROGRESS](MASTER_PROGRESS.md). Next: **P2-1.1 Design System** (shadcn primitives + SOC composites + `/design` gallery, Tailwind MCP). `[Claude · 2026-07-29]`
- **✅ Architect Decision — Architecture v1.0 APPROVED · Phase 1 CLOSED · Phase 2 AUTHORIZED.** The Principal Architect reviewed the core architecture, Phase-1 Exit Review, roadmap, future architecture, ADR collection, Constitution, and the productization plan, and ruled the platform **sufficiently mature to begin productization**. Effective immediately: transition from Architecture Phase to **Implementation Phase** — implementation now outranks documentation; speculative improvements are captured as ADRs/roadmap/tech-debt, not built mid-milestone. The decision **freezes** system architecture, service boundaries, event/API contracts, DB boundaries, tenant isolation, incident lifecycle, and alert flow (no change without Architect approval), and **freezes the frontend stack** as the official platform standard (React 19 · TS · Vite · Tailwind v4 + Tailwind MCP · shadcn/ui · Redux Toolkit · TanStack Query · React Router · RHF + Zod · Recharts · Lucide — no other UI kit). **P2-1 Operations Console is the active milestone.** Ratified governance: **[ED-0030](../project/ENGINEERING_DECISION_LOG.md) ✅ Approved**, **[ADR-0019](../adr/ADR-0019-operations-console-and-apps-layer.md) → Accepted** (`apps/*` workspace + `app` import-graph layer). Began **P2-1.0 Foundation**. Success criteria fixed (login → register RTSP camera → live video → upload → analyze → events → incidents → alerts → evidence → full operator walkthrough). Note: the `tailwindcss-mcp-server` is connected at the Claude Code CLI level but its tools aren't yet exposed to the active session tool registry (available: MCP_DOCKER, mui-mcp, claude-vscode) — a session restart will surface them; the MCP is wired into the design-system slice (P2-1.1), so P2-1.0 (scaffold + tokens authored from the frozen DESIGN_SYSTEM spec) is unaffected. `[Claude · 2026-07-29]`
- **Architecture Enhancement Review (documentation-only).** Forward-readiness review commissioned before building more of Phase 2. Produced **12 deliverables** under a new [`docs/architecture/future/`](../architecture/future/README.md) tree: capability roadmap, enhancement proposal (with a 10-principle compliance matrix + risks), **AI Capability Registry** catalog, **Analysis Profiles**, **AI Packs** strategy, **immutable Evidence Package**, **Human Review** feedback loop, **AI Benchmark** framework, **future Inference Scheduler**, and **demonstration workflows** (now P2-1's binding acceptance criteria). Core stance: **most of these are already anticipated by the frozen architecture** ([05] registry/scheduler, [12] evidence, [13] packs, [08] AI/ML, [19] perf) — the review **formalizes enterprise specs on top of existing seams**, changing **no code, no contract, and no frozen doc (01–28), and adding no service**; every spec ends "Not built now." On the mid-turn ADR suggestion: kept the **single existing four-digit ADR system** (a parallel `docs/architecture/adr/` would fragment governance + re-decide frozen choices) and added the genuinely-new decisions as **Proposed** ADRs — **0019** (console/`apps/` layer), **0020** (Evidence Package), **0021** (Analysis Profiles), **0022** (Capability Registry) — mapping the already-decided suggestions to their existing records. [ED-0031](../project/ENGINEERING_DECISION_LOG.md). All recommendations ⏳ Architect Review Pending. `[Claude · 2026-07-29]`
- **Phase 1 ✅ Architecturally Complete (Architect directive)** — all 8 slices approved. Pivot to **Phase 2 Productization**. Produced the **Phase-1 Exit Review** (9 dimensions — boundaries/contracts/event-flow/deps/TD/deferred/perf/security/ops — + 8 TD, 15 risks; **🟢 GO conditional**), and the **P2-1 Operations Console plan**: a dark **SOC console** on the Architect-mandated stack (React 19/TS/Vite/Tailwind v4 + Tailwind MCP/shadcn/ui/Redux Toolkit/TanStack Query/React Router/Lucide/Recharts/RHF+Zod). Delivered all 10 planning artifacts (UI architecture, routes, component hierarchy, **design system** with dark tokens + primitives, navigation, layout, folder structure, Tailwind organization, reusable-components list, page-dependency diagram) + state strategy (RTK ⊥ TanStack Query), API mapping (→ gateway `/api/:service/*`), and a 15-step milestone breakdown. Flagged the real work behind the "UI" milestone: **six backend enablers G-1…G-6** (browser live-preview, upload→analyze, media→inference + ONNX model, evidence refs, gateway CORS+SSE, incident assign/comments) — all **extensions to existing services, not new services**. New `apps/*` workspace layer + `app` import-graph boundary proposed via **[ED-0030](../project/ENGINEERING_DECISION_LOG.md)/ADR-0019**. **Planning only — no code; P2-1 ⏳ Architect Review Pending.** Docs: [PHASE1_EXIT_REVIEW](../project/PHASE1_EXIT_REVIEW.md), [OPERATIONS_CONSOLE](../architecture/phase2/OPERATIONS_CONSOLE.md), [DESIGN_SYSTEM](../architecture/phase2/DESIGN_SYSTEM.md); TD-9. `[Claude · 2026-07-29]`
- **P1-8 — Incident lifecycle + Alert engine (code complete, awaiting review). Closes the camera→alert vertical (M4).** Realized the two frozen bounded contexts the blueprint carves out (22/23) — merging them would break service ownership. **`@vip/service-workflow`** (Workflow context) consumes `incident.candidate` and **idempotently promotes** it to a `raised` Incident (unique `(tenant, source.dedupKey)` collapses a burst into one), then drives a validated **state machine** `raised→acknowledged→resolved→closed` (illegal moves 409; each transition versioned + appended to an immutable `history[]`) and publishes `incident.raised|acknowledged|resolved|closed`; incidents are **never created via the API** (only promoted). **`@vip/service-notify`** (Notification context — the **Alert Engine**) consumes **`incident.raised`** only — never the rule candidate (Architect rec 3) — selects the tenant's **enabled** channels meeting the incident's `minSeverity` floor, delivers per channel via a `ChannelSender` (**in-app + webhook** in Phase 1; email/SMS/push deferred), records a delivery log, publishes `notification.sent|delivered|failed`, and supports recipient **ack** (`notification.acked`, the with-ack completion); idempotent per `(tenant, incident, channel)`. **End-to-end `correlationId`** (rec 1): required on the Incident (the event's, else anchored to the event id), inherited by the Notification, and the events normalizer now defaults every event's `correlationId` to its own id. **Loop-free by construction** (rec 2's promotion + delivery on distinct roots): workflow filters `incident.candidate`/publishes sibling `incident.*`; notify consumes `incident.raised`/publishes onto the new `NOTIFICATIONS` stream. `@vip/contracts` +incident/notification schemas + catalog `incident.*`/`notification.*` (**30 schemas**); `@vip/messaging` +incident-lifecycle/notification subjects + `NOTIFICATIONS` stream; `@vip/permissions` +`notification:*`/`notification:ack`; gateway +`workflow`/`notify` upstreams. **45 slice tests** (19 workflow + 18 notify, incl. **5 real-Mongo**, + 8 contract). **Live-validated the full P1-6→P1-5→P1-7→P1-8 chain** (detection → event → candidate → raised incident → delivered notification → ack). Decision [ED-0029](../project/ENGINEERING_DECISION_LOG.md); deferrals (escalation/on-call/cases, workflow↔notify ack loop, email/SMS/push, retry/DLQ/rate-limits, evidence linking) → [TD-8](../../tracking/TECH-DEBT.md). Scenario [slice-013](../testing/slice-013.md). **Phase 1 code-complete (8/8) — M1+M2+M3+M4 done.** `[Claude · 2026-07-29]`
- **P1-7 — Rule engine ✅ Architect-approved** (highlights: EventEnvelope canonical, evaluation ⊥ incident, lifecycle/priority/metrics/sandboxed DSL, loop-free automation outputs). Its 3 forward recommendations were folded into P1-8. `[Claude · 2026-07-29]`
- **P1-5 — Event pipeline ✅ Architect-approved.** Its two recommendations landed as `EventEnvelope` evolution in P1-7: an explicit **`envelopeVersion`** (structure version, distinct from payload `schemaVersion`) + a coarse **`category`** (`perception`/`security`/`safety`/`system`/`analytics`) stamped from the catalog by the events normalizer. `[Claude · 2026-07-29]`
- **P1-7 — Rule engine (code complete, awaiting review).** Opened **M4 (Automation & Response)**. Built **`@vip/service-rules`** — the automation brain. Consumes `event.persisted` and evaluates tenant **versioned** rules over the **`EventEnvelope`** (never `DetectionResult` — Architect rec 1) via a **pure sandboxed predicate DSL**: a finite `all`/`any`/`not` tree of field/op/value leaves (eq/ne/in/nin/gt/gte/lt/lte/exists/contains) — **data, not code**: no `eval`, **no regex** (ReDoS-safe), no I/O, prototype-pollution-safe field access; deterministic (rec 5). **Evaluation is separated from incident creation** (distinct domain modules — rec 2): a match doesn't imply an incident. Rules carry an explicit **lifecycle** (draft/validated/enabled/disabled/archived) **+ priority** (rec 3, replacing a boolean), are **versioned + audited** (immutable `rule_versions`), and support **windowed thresholds** ("≥N within W", in-proc state behind a port). **Evaluation metrics** on `/metrics` (rec 4). CRUD kept lean (rec 6): create/get/list/update/delete/versions + `POST /rules/:id/dry-run` (no side effects), permission-gated + tenant-scoped. **Loop-free by construction**: the engine consumes `t.*.event.>` but publishes `incident.candidate`/`rule.matched` on a **distinct automation root** (`t.*.incident.>`/`t.*.rule.>`, new `AUTOMATION` stream) — so it structurally cannot self-trigger. `@vip/contracts` gains rules/incident schemas + the `EventEnvelope` evolution + catalog categories/`rule.matched`/`incident.candidate` (**25 schemas**); `@vip/messaging` gains the automation subjects/stream; gateway gains the `rules` upstream. **35 slice TS** (sandboxed interpreter, evaluator, CRUD+versioning+dry-run, engine E2E, permission/tenant routes, **2 real-Mongo**) + 4 contract tests. **Live-validated the full P1-6→P1-5→P1-7 chain** on the dev stack: a detection → normalized `perception.person.detected` event → rule match → `incident.candidate` (critical). Decision [ED-0028](../project/ENGINEERING_DECISION_LOG.md); in-proc rule state → [TD-7](../../tracking/TECH-DEBT.md). Scenario [slice-012](../testing/slice-012.md). `[Claude · 2026-07-29]`

## 2026-07-28

- **P1-5 — Event pipeline (code complete, awaiting review).** **Completes M3 (Perception & Event Backbone)** — a detection now becomes a persisted, deduplicated event on the backbone. Built **`@vip/messaging`**: a fail-closed NATS-JetStream client (`@nats-io` v3 modular, pure-JS, no install scripts — the deprecated `nats` v2 rejected) — tenant-partitioned subjects `t.{tenant}.…` with **structural tenant-token validation** (a `.`/`*`/`>` is refused, Law 5), an `EventBus` port (durable publish + pull-consume with explicit `ack`/`nak`/`term`→**DLQ**), an `InMemoryEventBus` for CI, and a `JetStreamEventPublisher` for the existing domain-event seam. Built **`@vip/service-events`**: consumes `t.*.capability.output.*` → **normalizes** each `DetectionResult`→`EventEnvelope` (label→domain-neutral catalog type; new generic `perception.object.detected` fallback) → **dedup+persist** tenant-scoped behind a unique `{tenantId,dedupKey}` index (`type+camera+zone+track/class+time-bucket`, [09 §Dedup]) → **re-publishes** `event.persisted` on `t.{tenant}.event.*`; `GET /events` (cursor-paged, permission+tenant-scoped) + bounded `POST /events/replay` (admin); anything without a valid tenant is **dead-lettered** (fail-closed). Wired the **capability→events edge over NATS subjects** (blueprint-consistent, not HTTP). `ai/inference` gained a lazy, integration-only **`NatsEventSink`** (`INFERENCE_EVENT_SINK=nats`, `nats-py`) — default stays the dependency-free null sink so the Python core + CI remain stdlib-only. `@vip/contracts` +`EventQuery`/`EventPage`/replay + `event.persisted` catalog (**21 schemas**); `@vip/permissions` +`event:replay`; gateway `events` upstream. **Live validation caught + fixed a real defect**: the events service now ensures BOTH streams at boot (publishing `event.*` with no capturing stream was silently NAK'd→deduped→dropped). **29 slice TS** (10 messaging + 19 events, incl. 2 real-Mongo) + 2 Python; **live-validated E2E** against dev-stack NATS+Mongo (publish→JetStream→persist→`GET /events`; duplicate collapsed to 1; other tenant sees 0; `event.persisted` republished; replay). Decision [ED-0027](../project/ENGINEERING_DECISION_LOG.md); event-publish half of [TD-5](../../tracking/TECH-DEBT.md) resolved; lifecycle-publisher retrofit deferred → [TD-6](../../tracking/TECH-DEBT.md). Scenario [slice-011](../testing/slice-011.md). **M1 + M2 + M3 code-complete.** `[Claude · 2026-07-28]`
- **P1-6 — AI inference / capability runtime (code complete, awaiting review).** Opened **M3 (Perception)**. Built the model-agnostic **AI Runtime Platform** in Python (`ai/inference`, stdlib-only core). Contract-first perception schemas in `@vip/contracts` (generic `Detection` + versioned `DetectionResult` + `FrameRef`; 20 JSON schemas). The **Principal Architect approved P1-6 with 8 platform improvements — all folded in**: (1) **capability manifests** + registry (zero-code registration/discovery), (2) **ModelAdapter** backend seam (runtime never imports onnxruntime; Fake + Onnx behind one interface), (3) **FrameContext**, (4) **model-independent `Detection`** (attributes/embedding/metadata/trackingId), (5) **lifecycle health states** (LOADING/READY/FAILED/DISABLED/UNAVAILABLE), (6) **metrics** from day one, (7) **version metadata** on every result, (8) **staged pipeline** (preprocess→infer→postprocess→track→translate→publish, each replaceable). Model bound by **selector** from the registry; frame without tenant **dropped fail-closed**; capability **persists no tenant data**. Backends: `stub` (deterministic, dependency-free default) + `onnx` (real onnxruntime + MLflow, integration-only, lazily imported). Stdlib `http.server` transport, `x-internal-key` gated. **40 slice tests** (38 Python stdlib incl. full `/infer` round-trip + 2 TS); **live-validated** end-to-end (frame → person detection, 0.84 conf, full provenance). CI `mlops` job now runs the inference suite. Decision [ED-0026](../project/ENGINEERING_DECISION_LOG.md); real-ONNX/event-publishing deferrals → [TD-5](../../tracking/TECH-DEBT.md). Scenario [slice-010](../testing/slice-010.md). `[Claude · 2026-07-28]`
- **P1-4 — RTSP ingestion + recording (code complete, awaiting review).** First **data-plane** slice. Built **`@vip/storage`** — the MinIO counterpart of `@vip/tenancy`: `TenantObjectStore` enforces the `{tenantId}/…` object prefix (fail-closed) over an AWS-SDK-v3 `S3ObjectStore` (pure-JS), signed-URL access. Built **`@vip/service-media`** — per-camera `StreamSupervisor` (connecting→connected→lost→…→stopped): resolves creds from the camera **internal endpoint** (new; `x-internal-key`, the one decryption point — gateway now strips that header), decodes via the **ffmpeg binary** (segments + JPEG frames), records under `{tenantId}/{cameraId}/recordings/…`, **auto-reconnects with exponential backoff**, degrades on storage failure, emits `media.stream.connected|lost` + `media.recording.segment`. All I/O behind ports → the lifecycle is fully fake-tested; the ffmpeg/RTSP path uses an opt-in dev-stack `media` profile (mediamtx + ffmpeg synthetic source). Also: `@vip/config` `storage`(recordings bucket)+`internal` groups, `@vip/permissions` `stream:*`/`stream:control`, camera internal resolve endpoint, gateway `media` upstream, `vip-recordings` bucket. **61 slice tests** incl. 4 real-MinIO — recording path **live-validated** against dev-stack MinIO. Decision [ED-0025](../project/ENGINEERING_DECISION_LOG.md); perception handoff + shared-key → [TD-4](../../tracking/TECH-DEBT.md). Scenario [slice-009](../testing/slice-009.md). **M2 (Ingestion) code-complete.** `[Claude · 2026-07-28]`
- **P1-3 — Camera registry + org hierarchy (code complete, awaiting review).** Opened **M2 (Ingestion)**. Contract-first camera schemas + `camera.registered|updated|removed|health.changed` events (15 JSON schemas total). Built **`@vip/crypto`** — build-free (`node:crypto`) authenticated **AES-256-GCM** credential vaulting (`SecretBox`, scrypt-derived key from `.env`, ADR-0018). Built **`@vip/service-camera`**: onboard cameras into the tenant-owned hierarchy (`zoneId` → `OrgNode`), **credentials sealed at rest and never returned** (`hasCredentials` only; URL-embedded creds rejected), permission-gated CRUD + health + ONVIF discovery stub (501), `camera.*` publisher seam. Verifies identity's Bearer token itself (`iss=identity`); gateway gains the `camera` upstream. Resolved the blueprint's "camera owns the hierarchy" wording against frozen [22 §1/§3] — **Tenant keeps the hierarchy, Camera references a zone** ([ED-0024](../project/ENGINEERING_DECISION_LOG.md); async zone integrity → [TD-3](../../tracking/TECH-DEBT.md)). **40 slice tests** (crypto tamper/round-trip, authz, credential-safety, **cross-tenant isolation**), incl. 4 real-Mongo — validated live. Scenario [slice-008](../testing/slice-008.md). `[Claude · 2026-07-28]`
- **P1-2 — Authentication + authorization (code complete, awaiting review).** Contract-first user/auth DTOs + auth events. Built **`@vip/auth`** (build-free: node `scrypt` passwords, `jose` HS256 access tokens, opaque SHA-256-hashed refresh tokens) and **`@vip/permissions`** (RBAC + wildcard PDP, deny-by-default). Extended **identity**: tenant-scoped users, `/auth/login` (tenant via `x-tenant-id`), `/auth/refresh` with **refresh-token reuse-detection** (replay → revoke family), `/auth/logout`, `/auth/me`, and permission-gated `/users`. Built **`@vip/service-gateway`** — the trust boundary: validates the token at the edge and reverse-proxies with injected `x-tenant-id/x-principal-id/x-roles`, **stripping client-supplied copies** (spoof-proof). **154 TS tests (160 w/ Mongo)**; identity auth vertical validated live against real Mongo. Decision [ED-0023](../project/ENGINEERING_DECISION_LOG.md); scenario [slice-007](../testing/slice-007.md). Also: adapters now honor the DB in `MONGO_URI`. **M1 (Access) is code-complete.** `[Claude · 2026-07-28]`
- **P1-1 — Tenant foundation + fail-closed isolation (code complete, awaiting review).** Contract-first: added tenant/org-hierarchy schemas + lifecycle event types to `@vip/contracts` (9 JSON schemas). Built **`@vip/tenancy`** — the fail-closed data-layer guard (`TenantScope` + pure `scopedFilter/scopedInsert/guardUpdate/scopedPipeline` + `TenantRepository<T>` over Mongo). Built **`@vip/service-tenant`** (Fastify, from the identity template): provisioning, org hierarchy, cross-tenant **403** gate, `/ready` Mongo check, `tenant.created` publisher seam (NATS deferred to P1-5). **62 TS tests**; validated **live end-to-end against real Mongo** (provision → self-read 200 → wrong-context 403 → org-node isolation → SIGTERM). Full root gate green. Decisions [ED-0021](../project/ENGINEERING_DECISION_LOG.md) (registry vs guarded data), [ED-0022](../project/ENGINEERING_DECISION_LOG.md) (dev-stack Mongo over Testcontainers). Scenario: [slice-006](../testing/slice-006.md). `[Claude · 2026-07-28]`
- **Phase 1 blueprint approved** by the Architect. Began implementation prep.
- **Documentation optimization pass** (Architect-directed): merged the 3 Phase-1 meta-docs (IMPLEMENTATION_ORDER, DEPENDENCY_GRAPH, DEFINITION_OF_DONE) into [README](../architecture/phase1/README.md) + [ROADMAP](../architecture/phase1/ROADMAP.md) — Phase 1 docs 15 → 12. README is now the navigation page; ROADMAP is lean (objective/deps/acceptance/status per slice + sequencing + per-milestone exit).
- **Established `docs/tracker/`** as the single canonical tracker: [MASTER_PROGRESS](MASTER_PROGRESS.md) (dashboard), [REVIEW_HISTORY](REVIEW_HISTORY.md) (per-slice, replaces the 7 `docs/review/SPRINT-*` files), this log (replaces per-day files under `docs/daily/`). Redirected the redundant Phase-0 dashboards to the new canonical. Recorded as [ED-0020](../project/ENGINEERING_DECISION_LOG.md).
- **Next:** implement **P1-1** — tenant foundation + fail-closed isolation guard, contract-first.
- `[Claude · 2026-07-28]`

## 2026-07-27

- **Phase 0 built end-to-end (Slices 0–5, P0-1…P0-7):** monorepo + dev stack (ADR-0016 NATS, ADR-0017 Fastify); `@vip/contracts` (Zod 4 + JSON-Schema codegen, 19 tests); CI quality gate + import-graph + contract harness; `@vip/service-identity` Fastify reference template (19 tests) + the **engineering-governance framework**; MLOps registry (MLflow + DVC, validated live then torn down, 5 py tests); `@vip/config` **`.env`-only secrets** (ADR-0018, 14 tests). **52 TS + 5 py tests; architecture frozen v1.0; Phase 0 complete.**
- **Phase 1 architecture planning:** re-scoped Phase 1 to a **camera → alert vertical** ([ED-0019](../project/ENGINEERING_DECISION_LOG.md)); wrote the full `docs/architecture/phase1/` blueprint + PROJECT_ROADMAP. Frozen architecture 01–28 unchanged.
- Per-slice detail: [REVIEW_HISTORY](REVIEW_HISTORY.md). Notable fixes that day: import-graph self-edge false-cycle; TS pinned 5.9.3 (TD-1, typescript-eslint); MLflow 3.x allowed-hosts (R-012); Prometheus per-instance registry.
- `[Claude · 2026-07-27]`

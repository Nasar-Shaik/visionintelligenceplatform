# Technical Debt Register

> The single place where known shortcuts, deferrals, and "we'll fix this later" decisions are recorded, so no agent rediscovers them the hard way. Adding debt without recording it here is a defect. Pay debt down by referencing its ID in the commit/PR that resolves it.

**How to use:** when you take a deliberate shortcut, add a row with an ID (`TD-NN`), what/why, the risk if left, and the trigger that should force paying it down. When you fix one, move it to **Resolved** with the resolving commit/PR. Sign entries `[<agent/name> · YYYY-MM-DD]`.

## Severity

`high` = correctness/security/scale risk; `medium` = maintainability/perf; `low` = cosmetic/ergonomic.

## Classification review — P-6.5 freeze · 2026-08-05

⚠️ **Debt is what an engineer notices. Anything a customer notices belongs on the roadmap**, where it
is planned, or in [KNOWN_LIMITATIONS](../docs/project/KNOWN_LIMITATIONS.md), where it is disclosed.
Every open row was re-read against that line at the freeze. Nothing was implemented; three rows moved.

| Row                                              | Verdict                                                                                                                                                                          |
| ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **TD-53** no delivery retry                      | ⚠️ **Promoted to the roadmap → P-7**, and disclosed as **L-32**. A customer's alerts are lost; that is product behaviour, not a shortcut                                         |
| **L-36** split acknowledgement                   | ⚠️ **Recorded and promoted → P-7** (new at this freeze). Two operators can both come away owning one incident; the console now names the other person, which is the honest floor |
| **TD-45 · TD-46 · TD-47 · TD-40 · TD-31 · TD-3** | Already on the roadmap as **P-6** items 10–14. Re-confirmed customer-visible; they stay listed here only so the engineering detail has a home                                    |
| **TD-50 · TD-51 · TD-54 · TD-55 · TD-56**        | Stay as debt. Each is mitigated and what remains is an engineering hazard — a wrong word during an outage, a bounded cost, a stale tool, a gate gap                              |
| **TD-27 · TD-28 · TD-29 · TD-43 · TD-48**        | Stay as debt **and** are already disclosed as limitations. The debt row carries the engineering account; the limitation carries the customer's                                   |

## Open debt

| ID   | Area              | What / why (the shortcut)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Risk if left                                                                                                                                                                        | Pay-down trigger                                                                                                                                                                                                                                               | Sev    |
| ---- | ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| TD-1 | toolchain         | TypeScript pinned to **5.9.3** instead of latest **7.0.2**, because `typescript-eslint` (latest 8.65) refuses TS 7.0 and type-aware linting would break. `[Claude · 2026-07-27]`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | On latest-minus-one major of TS; miss TS7 native-compiler speed/features                                                                                                            | typescript-eslint ships TS 7 support (typescript-eslint#10940) → bump TS to 7.x via ADR + rerun full gate                                                                                                                                                      | low    |
| TD-2 | docs              | Tracker consolidated into `docs/tracker/` ([ED-0020](../docs/project/ENGINEERING_DECISION_LOG.md)), but the `docs/project/` + `docs/ai/` status files (CURRENT_STATUS, CURRENT_SPRINT, NEXT_STEPS, BACKLOG, DECISIONS, etc.) were left in place (redirected only where they'd break). Some still carry stale Phase-0 snapshots. `[Claude · 2026-07-28]`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Duplicate/stale status surfaces; a future reader may trust an out-of-date file                                                                                                      | Next doc-touching slice: reduce them to thin pointers to `docs/tracker/` + governance registers, or remove the redundant ones                                                                                                                                  | low    |
| TD-3 | camera            | A camera's `zoneId` is validated for shape + tenant-scope but **not** confirmed to reference an existing `OrgNode` — cross-context existence checking is deferred to async reconciliation over `tenant.hierarchy.changed` rather than a synchronous camera→tenant call ([ED-0024](../docs/project/ENGINEERING_DECISION_LOG.md)). `[Claude · 2026-07-28]`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | A camera can be onboarded against a non-existent/deleted zone id until the backbone reconciles; orphaned cameras                                                                    | P1-5 event backbone: camera subscribes to `tenant.hierarchy.changed` and rejects/repairs orphaned `zoneId`s (and validates on create against a local hierarchy cache)                                                                                          | medium |
| TD-4 | media             | ~~Extracted frames go to a **null perception sink**~~ (✅ **resolved P-8 Phase 2**: `HttpFrameSink` delivers to the runtime, bounded per camera, non-blocking, fully accounted) and media stream state is still **in-memory** (per-camera workers; lost on restart). Media→camera credential resolve is authenticated by a **shared `INTERNAL_API_KEY`**, not a per-service machine principal. Recording metadata is not persisted (listed from MinIO). `[Claude · 2026-07-28]` ([ED-0025](../docs/project/ENGINEERING_DECISION_LOG.md))                                                                                                                                                                                                                                                                                                           | Frames aren't consumed until P1-6; a restart drops worker state (re-derivable); a leaked shared key authorizes any internal caller                                                  | P1-6 wires the real frame bus (drop-to-latest); a machine-principal/mTLS replaces the shared key; persist stream/recording state if restart-continuity or query needs demand it                                                                                | medium |
| TD-5 | inference         | ~~The inference runtime ships with the **`stub` backend as the tested default**; the real `onnx` backend is integration-only (no registered ONNX model)~~ **RESOLVED P-8 Phase 3 (2026-08-05):** the deployed runtime runs `onnx` with **YOLOX-nano registered and checksum-verified inside the image**, resolved from a local catalogue rather than MLflow — a production start no longer depends on a tracking server being reachable. Verified against the deployment: 2 detections on a photograph of 2 people, **0** on a test pattern; mutation-tested three ways. ⚠️ **What remains is model-CI**: there are still no FP/FN promotion gates against a labelled corpus, so the platform can say _inference runs_ and cannot say _how well_. Split out as **TD-64**. `[Claude · 2026-08-05]`                                                  | —                                                                                                                                                                                   | —                                                                                                                                                                                                                                                              | —      |
| TD-6 | events            | The `event.persisted` **perception** path is wired end-to-end (inference → NATS → events → persisted → `t.{tenant}.event.*`), but the **lifecycle producers (tenant/camera/media) still use the `LoggingEventPublisher` default** — their `tenant.*`/`camera.*`/`media.*` events are logged, not published on the backbone. `@vip/messaging`'s `JetStreamEventPublisher` is delivered + unit-tested but **not wired at boot**, to avoid making three control-plane services NATS-hard-at-boot without a focused review. `[Claude · 2026-07-28]` ([ED-0027](../docs/project/ENGINEERING_DECISION_LOG.md))                                                                                                                                                                                                                                           | Lifecycle events aren't yet durable/replayable; a future rules/analytics consumer can't react to them; the async zone-integrity reconciler (TD-3) has no backbone events to consume | Retrofit each producer's composition root onto `JetStreamEventPublisher` (fail-fast or fail-soft, decided at review); add the TD-3 `tenant.hierarchy.changed` consumer; then wire media→inference→events (TD-4) for the full vertical                          | medium |
| TD-7 | rules             | The rule engine's **windowed-threshold state is in-process** (`InMemoryRuleStateStore`, per-instance sliding window) rather than Redis; and the engine does a **Mongo `listEnabled` query per event** (no per-tenant enabled-rule cache). Both are behind ports (`RuleStateStore` / `RuleStore`). `[Claude · 2026-07-29]` ([ED-0028](../docs/project/ENGINEERING_DECISION_LOG.md))                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Windowed counts don't span replicas + reset on restart (re-derivable via event replay); per-event rule fetch adds DB load at scale                                                  | Add a `RedisRuleStateStore` (sorted-set + TTL, tenant-prefixed) behind the port; cache enabled rules per tenant with short-TTL invalidation on CRUD; partition consumers by tenant/camera                                                                      | medium |
| TD-8 | workflow / notify | P1-8 ships the **incident-lifecycle subset** of the Workflow context (`raised→acknowledged→resolved→closed`); **escalation policies + timers, assignment/on-call, cases, and a hash-chained audit log are deferred** (audit is an embedded `history[]`, not hash-chained). The **workflow↔notify feedback loop is not wired** (`notification.acked` does not auto-acknowledge the incident; ack is operator-API-driven). The Alert Engine ships **`in-app` + `webhook`** senders only — **email/SMS/push are not implemented** (`ChannelSender` seam ready). **Delivery retry/backoff + DLQ + per-tenant rate limits are not implemented** (a failed delivery is recorded `failed`, not retried); evidence-ref linking (clip/snapshot from `media`) is not wired. `[Claude · 2026-07-29]` ([ED-0029](../docs/project/ENGINEERING_DECISION_LOG.md)) | No escalation if an incident is never acked; a failed notification isn't retried; alerts can't yet reach email/SMS; no evidence attached                                            | Add escalation policies + timers + on-call/assignment; wire `notification.delivered\|acked` → workflow auto-transitions; add email/SMS/push `ChannelSender`s; add delivery retry/backoff + DLQ + rate limits; link `media` evidence signed-URLs onto incidents | medium |

| TD-9 | productization | **Backend enablers for the Operations Console (P2-1)** — six extensions to existing services needed for the demo success criteria, none yet built: **G-1** media browser live-preview (MJPEG/snapshot); **G-2** media recorded-video **upload→analyze job** (file source → ffmpeg → frames); **G-3** wire **media→inference** frame bus + register one **ONNX model** (subsumes TD-4/TD-5 for the live/recorded path); **G-4** incident **evidence refs** (snapshot/clip signed-URLs on raise); **G-5** gateway **CORS + SSE** live feed; **G-6** incident **assign + comments**. All are service extensions, **not new services**. `[Claude · 2026-07-29]` ([ED-0030](../docs/project/ENGINEERING_DECISION_LOG.md); [PHASE1_EXIT_REVIEW](../docs/project/PHASE1_EXIT_REVIEW.md) §11) | Console can't show live video, analyze uploads, or display evidence until these land; demo blocked | Schedule each as a reviewed increment sequenced ahead of its consuming console module (OPERATIONS_CONSOLE §13/§14); G-3 also pays down [TD-4](TECH-DEBT.md)/[TD-5](TECH-DEBT.md) | high |
| TD-10 | events / versioning | **Event versioning strategy documented but no cross-major upcaster.** `EventEnvelope` carries `envelopeVersion` (structure) + `schemaVersion` (payload); strategy defined in [EVENT_SPINE_SEMANTICS §1](../docs/architecture/future/EVENT_SPINE_SEMANTICS.md). Only a single major (`1.0.0`) exists; the pure `upcast(envelope)` seam that lets a future major replay historical events is **not** built. `[Claude · 2026-07-30]` | A future breaking envelope change could break **replay** of historical events (G-3.5 determinism) if consumers can't parse the old major | Before any `envelopeVersion` **major** bump (+ADR): add a pure/total upcaster at ingest+replay so every persisted major normalizes to the current in-memory model | low |
| TD-11 | events / dedup | **Deduplication is layered + identity-based** (broker `msgId`=eventId ⊕ events-store content+time-window key ⊕ rule+group+window incident key) — **not** correlationId, **not** content-hash ([EVENT_SPINE_SEMANTICS §2](../docs/architecture/future/EVENT_SPINE_SEMANTICS.md)). Two edge cases are unhandled: near-identical events straddling a time-bucket edge aren't collapsed; multi-node producers with clock skew weaken the `occurredAt`-keyed content window. `[Claude · 2026-07-30]` | In distributed/edge deployments, boundary-straddling or skewed duplicates may create two events/incidents | Multi-node/edge producer rollout: synchronize producer clocks (monotonic source), document dedup-window vs. stream-retention together, and consider a content-hash tiebreaker if duplicates are observed | low |
| TD-12 | events / ordering | **Ordering guarantees documented** ([EVENT_SPINE_SEMANTICS §3](../docs/architecture/future/EVENT_SPINE_SEMANTICS.md)): per-subject FIFO + at-least-once + idempotent consumers; **no global total order**; only windowed-threshold rules are weakly order-sensitive (time-based, tolerant). No enforcement exists for a future stage that needs strict per-track order. `[Claude · 2026-07-30]` | A future stateful stage (Object Tracker / Behaviour Analyzer) added without per-unit partitioning could miscount under reorder/redelivery | When the tracker/behaviour analyzer lands (TD-14): pin it to a per-`(tenant,camera,track)` partition/consumer so order holds within the unit that needs it | low |
| TD-13 | inference / adapter | **Inference Adapter boundary exists but the label→event-type mapping is hardcoded, not manifest-driven.** `ModelAdapter`+`EngineRegistry`+`detections_to_events` give an engine-agnostic seam ([AI_PROCESSING_PIPELINE §2](../docs/architecture/future/AI_PROCESSING_PIPELINE.md)), but the Node `event-normalizer` label map covers only person/vehicle/fire/smoke and the Python `events.py` map is code, so behaviour/safety/security/analytics catalog types aren't reachable via the `DetectionResult` path; system-event producers are also unwired (subsumes the G-3.5 report's TD-1/TD-2). `[Claude · 2026-07-30]` | New models/behaviours can't reach the full catalog without editing normaliser code; `system.*` lifecycle events have no ingest path | Before real multi-model integration: drive the label→type mapping from each capability's manifest + G-3 `ModelCapabilityProfile.supportedEventTypes`; wire camera/media/inference `system.*` producers onto the backbone | medium |
| TD-14 | inference / behaviour | **No stateful Behaviour Analyzer stage.** Behaviour catalog types (`behavior.loitering/theft/fight/fall`, `analytics.queue.length`, …) exist (G-3) but nothing produces them from **sustained, windowed state** over frames/tracks; G-3.5 models them via the producer door. Architectural direction in [AI_PROCESSING_PIPELINE §3](../docs/architecture/future/AI_PROCESSING_PIPELINE.md). `[Claude · 2026-07-30]` | Behaviour analytics (loitering/theft/fight/queue-trend) can't be produced from live streams until the analyzer exists | AI behaviour-analytics enabler: add a manifest-declared **Behaviour Analyzer** capability (per-track/zone windowed state, in-proc → Redis for HA, recovery via replay) that emits standardized `EventEnvelope`s; keep the event spine stateless | medium |
| TD-15 | evidence | **Live media-backed evidence extraction is deferred.** G-4 ships the `incident.raised` → evidence wiring + a **no-op** `IncidentEvidenceExtractor`; capturing a real key-frame snapshot / ring-buffer clip around an incident needs the Media frame source (TD-4), so auto-extraction currently no-ops. Evidence is registered explicitly via the API (producers/operators). `[Claude · 2026-07-30]` | No auto-generated evidence from live incidents until the media source is wired | Media ring-buffer/frame-source enabler: implement a Media-backed extractor that captures + registers a snapshot/clip on `incident.raised`, preserving Event→Incident→Evidence traceability | medium |
| TD-16 | evidence | **Export packages not implemented.** The frozen Evidence context includes signed/watermarked **export bundles** (clips + snapshots + metadata + custody log + JSON manifest, [12 §6](../docs/architecture/12-EVIDENCE-MANAGEMENT.md)); G-4 delivers the manifest + custody + query that COMPOSE an export but not the packaging/watermark/approval flow. `[Claude · 2026-07-30]` | Investigators can't yet hand off a defensible bundle to authorities | Export enabler: assemble the manifest + custody + media into a signed, watermarked package behind a permission-gated, audited, approvable action (workflow) | low |
| TD-17 | evidence | **Legal-hold approval workflow + redaction deferred.** G-4 supports a `legalHold` boolean + audited place/release; the full **hold approval/case** flow and **redaction** (auto-blur non-subjects for export) from [12 §6/§8](../docs/architecture/12-EVIDENCE-MANAGEMENT.md) are not built. `[Claude · 2026-07-30]` | No approval trail on holds; no redaction for privacy-safe export | Evidence-governance enabler: hold approval via workflow; a redaction provider seam on export | low |
| TD-18 | evidence | **Automated retention sweeps + tier transitions deferred.** The model carries `retainUntil`/`legalHold`/`StorageTier` and an `expireIfDue` domain gate, but there is **no scheduled sweep** that expires/purges due items or moves bytes across hot→cold→archive tiers ([12 §7](../docs/architecture/12-EVIDENCE-MANAGEMENT.md)). `[Claude · 2026-07-30]` | Past-retention evidence is not auto-expired/purged; storage cost not tiered | Retention scheduler: a periodic sweep calling `expireIfDue` + provider-side lifecycle/tier moves (verifiable purge with custody entry) | medium |

| TD-19 | gateway / realtime | **Real-time delivery hardening deferred (all additive behind G-5 seams).** G-5 ships SSE fan-out with a **bounded ring-buffer reconnect replay**, a **transport-agnostic `ConnectionSink`** (SSE adapter only), **priority-drop** backpressure, and **single-instance** operation. Deferred: (a) **durable JetStream replay** beyond the ring buffer; (b) **WebSocket + gRPC** transport adapters behind `ConnectionSink`; (c) **multi-instance gateway fan-out** (sticky sessions / shared cursor so replay + connection ceilings hold across replicas); (d) **low-priority event coalescing/throttling** at the documented drop seam ([REALTIME_DELIVERY §5/§7/§12](../docs/architecture/phase2/REALTIME_DELIVERY.md)). `[Claude · 2026-07-31]` | Deep catch-up limited to ring-buffer window; SSE-only transport; single gateway instance; no analytics coalescing | Per enabler: durable per-tenant replay consumer behind the ring-buffer API; WS/gRPC adapters; a shared cursor/store for horizontal scale; a coalescing policy at `classifyPriority`/enqueue | low |
| TD-20 | evidence / hierarchy | **Evidence resolves a location's ancestry as it is _now_, not as it was when the event occurred.** Evidence references the camera (whose `zoneId` is mutable) and a location's breadcrumb is derived from today's `path`, so an investigation into a six-month-old incident is told, with a complete breadcrumb, that it happened somewhere it did not. Nothing errors and no reference dangles — the **answer changes**, silently. Found by the P-3 Hardening review ([ED-0053](../docs/project/ENGINEERING_DECISION_LOG.md)); ownership is the **Evidence context**, not the Location Hierarchy, which is behaving correctly in both halves. `[Claude · 2026-08-02]` | A historical investigation can be confidently wrong about where an event happened; the error is undetectable from the record itself | **[E-1](../docs/tracker/E-1-EVIDENCE-LOCATION-SNAPSHOT.md)** — freeze `zoneId` + `path` on the evidence record at write time (additive, the pattern `ProbeConfigurationSnapshot` already uses); render "location not captured" for pre-E-1 records rather than today's location. The P-3 test asserting current behaviour is written to **fail** when this lands. | medium |
| TD-23 | rules / workflow | **An incident cannot be searched or grouped by behaviour id.** P-5's stated incident search includes **Behavior** and **Composite**, but `IncidentCandidate` carries only `ruleId`, `ruleVersion` and `ruleName` — no composite-behaviour id and no behaviour-profile id. The filters were therefore **not** added to the frozen `IncidentQuery`: one that always matches nothing is worse than an absent one, because it looks like an answer. Behaviour search is served today by `eventType` (`behavior.loitering`, …) and `category`. Found while implementing [P-5 entry criterion G-3](../docs/tracker/P-5-ENTRY-CRITERIA.md). `[Claude · 2026-08-03]` | Behaviour analytics cannot be reported on or grouped per composite/profile — the customer-facing question "how often does _this behaviour_ fire" is unanswerable without reading rule names | **Owner:** Rules context (producer change). **Priority:** medium. **Acceptance:** `IncidentCandidate` carries the composite/profile id that raised it; `Incident.source` carries it forward; `IncidentQuery` gains the filter **with** its index and a coverage-test row. **Blocking:** P-5 reporting only — not P-5 start. **Estimate:** one slice, additive throughout | medium |
| TD-24 | tenant / CI | **A timing-based scale assertion is flaky under parallel load.** `services/tenant/test/hierarchy-extensibility.test.ts` → "scales linearly rather than quadratically" failed once in three consecutive full-suite runs during P-5.0 and passed every time the package was run alone — including against a clean checkout with this slice stashed, so it is **not** caused by P-5.0. The assertion compares wall-clock ratios, and `turbo run test` runs twenty packages concurrently. `[Claude · 2026-08-03]` | An unrelated slice gets a red gate roughly one run in three, which trains everyone to re-run rather than read the failure — the way a real regression eventually ships | **Owner:** Tenant context. **Priority:** medium. **Acceptance:** the shape assertion no longer depends on wall-clock time — count operations (comparisons, store calls, nodes visited) rather than milliseconds, as `services/rules/test/scale.test.ts` does. **Blocking:** nothing; it is a CI-confidence defect. **Estimate:** small, contained to one test | medium |
| TD-26 | platform / permissions | **`*:read` silently grants every future read permission to every viewer.** `operator` and `viewer` both hold the `*:read` wildcard, so naming a new permission `<resource>:read` grants it to the least privileged role **at the moment it is spelled** — no grant is written, nothing fails, and no test asserts the negative. Found while adding the access audit in P-5.2.0: `audit:read` would have handed a log of _who viewed what, when, from which IP_ to every viewer in the tenant. Worked around by naming the action `audit:inspect` (off the wildcard) and pinning the hazard with a test that asserts `can(viewer, 'audit:read') === true`. `[Claude · 2026-08-03]` | Any future sensitive read permission is granted to viewers by default. The workaround depends on every future author knowing to check the expansion — which is exactly the kind of knowledge that does not survive a team change | **Owner:** Platform Core. **Priority:** medium. **Acceptance:** either replace `*:read` in the `operator`/`viewer` grants with an explicit resource list (**breaking** — needs an ADR and a migration note for any tenant relying on it), or add a `SENSITIVE_PERMISSIONS` register that `can()` refuses to satisfy via a wildcard. **Blocking:** nothing today; the audit permission is safely named. **Estimate:** small for the register, medium for the narrowing | medium |
| TD-27 | playback / QA | **No surveillance hardware has ever been used to validate playback.** P-5.6 measured five browser engines and real generated H.264, but no Hikvision, Dahua, CP Plus, UNV or ONVIF device, no NVR archive and no vendor export has been played. Container and codec quirks (proprietary `.dav`, non-standard MP4 brands, HEVC by default) are exactly what a capability matrix cannot predict. **Fix:** check one real export per vendor into a fixture store; Hikvision and Dahua alone cover most of the installed base. See `docs/review/p56/NVR_VALIDATION.md`. |
| TD-28 | playback | **No live camera view exists, and it is not a partial feature.** No browser plays RTSP, so live playback requires a server-side repackager (HLS/LL-HLS/WebRTC/fMP4-over-WebSocket) that this platform does not have. The workspace reports live view as `not-built`. **Fix:** an ADR and a service-sized decision, not a milestone task. |
| TD-29 | playback / QA | **H.265 was never decoded, only probed.** No HEVC encoder was available locally, so support is known from `canPlayType`/`isTypeSupported` — which is what the product's check consumes — but no real H.265 file has been played in any engine. The measured engine split (Chromium refuses, Chrome/Firefox/Safari accept) is therefore a capability claim, not a decode result. **Fix:** one real HEVC fixture. |
| ~~TD-30~~ | console / QA | **RESOLVED in P-5.7** — Edge installed and measured; see `docs/review/p57/BROWSER_MATRIX.md`. ~~ **Microsoft Edge has never been measured.** It is not installed on the build machine and Playwright's `msedge` channel cannot launch. Edge is Chromium-derived _with different proprietary-codec licensing_, which is the exact axis on which the measured engines disagree, so its behaviour is unknown rather than assumed. **Fix:** run the P-5.6 probe on a machine with Edge. |
| TD-31 | console | **Two horizontal sliders are 24 px tall on touch, not 44 px.** The scrubber and the volume control meet WCAG 2.5.8's 24 px minimum and are 330–752 px wide, so they are easy to hit along their long axis, but they do not reach the 44 px target the buttons do. Measured on iPad and Pixel emulation. **Fix:** a custom slider with a padded hit area, since a native `input[type=range]` cannot separate track height from target height portably. |
| ~~TD-32~~ | deploy / QA | **RESOLVED in P-5.8** — the full stack now deploys as production runs it (Docker images, Caddy edge, HTTPS/CSP/compression/cache headers, health + readiness), and doing so found nine defects including completely broken playback. See `docs/review/p58/`. ~~ **Production deployment has never been exercised.** P-5.7 ran the full stack, but as `pnpm dev:*` against containerised infrastructure: no reverse proxy, no HTTPS, no CSP, no compression, no cache headers, no container images for the services themselves. Everything verified is therefore true of a development topology. **Fix:** a production compose (or chart) plus a verification pass over proxy, TLS, CSP, compression, cache headers, logging and health endpoints. See `docs/review/p57/VERIFICATION.md`. |
| TD-33 | console | **`shortId` and similar formatters crash on a missing required field.** A contract-required field absent from a stored row throws inside render. This is now _contained_ (route and panel boundaries) rather than fatal, and is deliberately **not** defended field-by-field: making every accessor optional-chained would hide genuinely invalid data instead of surfacing it. **Fix (if wanted):** validate rows at the API client boundary and report invalid ones explicitly, rather than degrading silently. |
| TD-34 | evidence | **Access-token expiry during playback was not forced.** The _signed-URL_ expiry path is implemented, unit-tested and exercised; a genuine bearer-token expiry mid-session was not. **Fix:** shorten the token TTL in a test deployment and drive a session across the boundary. |
| TD-35 | deploy | **Service images carry the whole built workspace, not a pruned bundle.** `pnpm deploy` would produce a much smaller image but rewrites the workspace layout in ways that must be re-verified per service; P-5.8 was about verifying a deployment, not optimising one. Images are ~1 GB each. `[Claude · 2026-08-04]` | Slow pulls, wasted registry and disk; dev dependencies present at runtime | A release that ships images to a registry, or a host where image size matters → `pnpm deploy --prod` per service + re-run the P-5.8 verification | low |
| TD-36 | console / security | **The CSP needs `style-src 'unsafe-inline'`.** React sets inline `style` attributes for layout the timeline computes at runtime (playhead position, gap widths, density-bar heights). That is geometry, not markup, but a browser cannot tell the difference. `[Claude · 2026-08-04]` | The one genuine relaxation in an otherwise strict CSP; an injected `style` attribute would execute | A nonce plumbed through every inline style, or moving computed geometry to CSS custom properties set on a nonce'd stylesheet | medium |
| TD-37 | console | **Zod v4's JIT probe trips the CSP on every page load.** Zod calls `new Function("")` inside a `try/catch` to detect codegen; the CSP denial is the answer it wants and it falls back to interpreted validation. Correct behaviour, but it logs a violation in Chromium and Firefox. `[Claude · 2026-08-04]` | Console noise that could mask a real CSP violation; slower validation than the JIT path | Zod exposing an opt-out for the probe. **Never** by adding `'unsafe-eval'` — that trades a real boundary for a quieter log | low |
| TD-38 | deploy / data | **Backups are per-collection consistent, not point-in-time.** `mongodump` runs against a single node without `--oplog`, so a backup taken mid-write can hold an incident without its notification. Proven to restore correctly; the consistency limit is inherent, not a bug. `[Claude · 2026-08-04]` | A restore can land in a state no single moment produced | A deployment that needs it: run MongoDB as a replica set and add `mongodump --oplog` + `mongorestore --oplogReplay` | medium |
| TD-39 | deploy / security | **No rate limiting at the edge.** The gateway authorizes every request but does not throttle; Caddy is configured for TLS, headers and routing only. `[Claude · 2026-08-04]` | An internet-facing deployment can be brute-forced on `/api/identity/auth/login` or flooded | Before any internet-facing installation: a rate-limit module at the edge, keyed per-IP for auth routes and per-tenant elsewhere | medium |
| TD-40 | console / UX | **Tenant must be typed by hand at login.** An operator has to know and enter a tenant slug before their email. Deliberate for multi-tenant correctness, but it is the first thing a customer sees and the one field they cannot guess. `[Claude · 2026-08-04]` | Avoidable friction at the very first interaction; support calls on day one | A product decision: derive the tenant from the hostname, remember the last one used, or offer a picker after email lookup |
| TD-41 | console / demo | **The demo dataset is invented, and must never be mistaken for a customer's.** Four fictional tenants (`tnt_demo_*`) with fictional sites, cameras and incidents; recordings are ffmpeg test patterns. Evidence custody and integrity hashes are genuine (registered through the real API); AI output and camera probe measurements are deliberately NOT fabricated. `[Claude · 2026-08-04]` | A screenshot of demo data could be read as a real deployment; a reviewer could mistake seeded incidents for measured ones | Only if the dataset is ever used outside demonstration and UI review. Marked in the seed output, `DEMO_DATASET.md`, and the tenant ids themselves | low |
| TD-42 | console / branding | **Branding is per-deployment, not per-tenant.** `branding.json` is read before sign-in — which is what lets the login screen carry the customer's identity — so a single deployment serving several customers cannot show each their own brand. `[Claude · 2026-08-04]` | A multi-tenant reseller deployment shows one brand to everyone | A reseller or multi-brand requirement: branding moves to the tenant record and applies after authentication, with the login screen falling back to deployment branding |
| TD-43 | console / design | **Dark theme only.** Deliberate for a SOC product, and the token layer would support a light palette — but none has ever been produced or reviewed, so there is no light theme to enable. `[Claude · 2026-08-04]` | Customers in bright environments, or with an accessibility requirement for light mode, cannot be served | A customer requirement: produce a light palette against the existing tokens and run the P-5.9 review at all five viewports in both modes | low |
| TD-45 | console / shell | **The shell does not respond below `md`.** The sidebar is 240 px at every viewport width including 390 px, leaving 150 px for the whole application; the top bar needs 270 px, so the clock, connection indicator and **user menu paint off-screen — an operator cannot reach Sign out on a phone**. A "Collapse" control exists, so the collapsed state is built; it is simply not automatic. Measured, [UI_BENCHMARK G-3](../docs/review/roadmap-2026-08/UI_BENCHMARK.md). **Re-measured in P-6.2 with descendants counted: 13 clipped elements at 390 px and 2 at 768 px — identical on `/users`, `/rules` and `/incidents`, every one of them inside the top bar's `ml-auto` cluster.** ⚠️ It is one defect in one component, not eleven broken pages, and it turns the responsive gate red on **every** page — which is how a gate stops being read. `users-ui.mjs` therefore attributes clipping to shell or page and fails only on the page's count. `[Claude · 2026-08-04]` | Phone and small-tablet use is broken, not merely cramped. Tablet (768 px) is affected too. ⚠️ **It also masks the next real page-level overflow defect** | P-6. Auto-collapse below `md` plus an off-canvas drawer — a design change, not a patch | med |
| TD-46 | console / shell | **The global search box is inert.** `Topbar.tsx` renders an `Input` with a placeholder and an `aria-label` but no `value`, `onChange`, form or handler, in the centre of the top bar on every screen. A customer types into it in the first minute of a demo. ⚠️ It also contradicts the platform's own discipline: the workspace declares search `not-built` while the top bar shows a search box that looks like it works. `[Claude · 2026-08-04]` | Highly visible in every demonstration; erodes trust in every other honest state the product shows | P-6. Either wire it to filter the queue and estate (both already client-side) or render it disabled with a stated reason. **Never ship an input that eats keystrokes** | med |
| TD-47 | console / tables | **Tables render rows and nothing else.** No column sort, bulk selection, result count, pagination control or column configuration on Incidents, Cameras or Events. Invisible at 6 rows; it is the product at 600. `[Claude · 2026-08-04]` | The estate size where the product is worth buying is the estate size where the tables stop working | P-6 for sort + result count; bulk actions follow the permission model | med |
| TD-48 | tenant | **A tenant's lifecycle status is stored and read by nothing.** `TenantStatus` (`provisioning · active · suspended · deprovisioning`) is persisted and validated, but no code path in identity, the gateway or `@vip/tenancy` refuses a request because a tenant is suspended — grepped across every service in P-6.3. The console therefore shows it **read-only**: a Suspend control would claim to lock a tenant out and do nothing. `[Claude · 2026-08-04]` | A reseller cannot suspend a non-paying tenant; and the field invites a future contributor to build a control on top of an unenforced value | P-14, with licensing and entitlements — refusing a tenant at the boundary is the same mechanism. Until then the read-only treatment is the honest one (L-24) | med |
| TD-49 | tenant / audit | **Settings changes are audited to the service log, not to a queryable store.** `tenant.updated` now carries before/after, actor, correlation id and timestamp — verified in the deployment — but the tenant service still uses `LoggingEventPublisher`, so it is a structured log line. `AccessAuditEntry` is frozen in the contracts with **no consumer and no route on any service**. ⚠️ Before P-6.3 a rename emitted nothing at all. `[Claude · 2026-08-04]` | "Who renamed the organisation, and when?" is answerable only from log retention. An auditor will not accept that in writing | P-13, the access-audit surface. The event is already emitted in the shape that consumer will want, so wiring it changes the sink and not the call site | med |
| TD-50 | services / readiness | **Every service's Mongo readiness check blocks instead of failing fast.** Measured in P-6.4 by pausing MongoDB against the deployment: all ten services' `/ready` calls hung, the gateway's 2-second probe budget expired, and every service was reported `Unavailable` — the same word a stopped container gets. The check should return `fail` within a bounded time rather than waiting on the driver's own timeout. `[Claude · 2026-08-04]` | An operator troubleshooting a hung database sees ten unavailable services rather than one hung dependency (L-29). The health page mitigates it by keeping the dependency's own row as `Unknown`, but the services' verdict is still the wrong word | Give each `readiness.register('mongo', …)` an explicit deadline. One change repeated in ten composition roots; no contract moves | med |
| TD-51 | console / routing | **A console route can be shadowed by the edge and nothing notices.** `/health` was a console route for two milestones while the edge proxied that exact path to the gateway's liveness probe, so the browser was handed `{"status":"ok"}` and the SPA was never reached — the placeholder was as unreachable as the page that replaced it. ⚠️ `verify.mjs` reported the route as rendering the whole time, because JSON logs no console errors and shows no crash boundary. Fixed for this case (`/system`, plus a shell assertion in the route walk), but nothing **structurally** prevents the next collision: the Caddyfile's `handle` blocks and `router.tsx` know nothing about each other. `[Claude · 2026-08-04]` | A route added to the console can silently never render in production, and the check that exists to catch that would pass | Assert the two lists against each other — the edge's explicitly-handled paths must be disjoint from the router's. A single test over both files | med |
| TD-53 | notify / delivery | **A failed delivery is never retried, and a delivery interrupted mid-flight is stranded for good.** Measured in P-6.5 by driving the real Alert Engine over four real transports: every delivery — successful or failed — has `attempts: 1`. There is no retry loop; `markFailed` is terminal. Worse, the fan-out's idempotency guard means the redelivered `incident.raised` **skips** any channel that already has a record, so a channel that was down for one minute never receives that alert, and a delivery left `pending` by a process that died between its insert and its hand-off stays `pending` permanently. Nobody is told, and nothing anywhere tries again. `[Claude · 2026-08-04]` | A customer's SOC webhook that blips for thirty seconds loses those alerts silently. The console shows the failure with its reason (P-6.5), so it is visible — but visibility is not delivery, and there is no control anywhere that re-sends | A retry with backoff belongs in the Alert Engine, bounded and recorded in `attempts` (the field and its UI already exist and currently never exceed 1). ⚠️ It needs the idempotency guard to distinguish "already delivered" from "already attempted" — the guard is on `(incident, channel)` existence, not on outcome | **high** |
| TD-54 | console / inbox | **The Inbox refetches every loaded page on every poll.** An infinite query re-requests all accumulated pages on each interval, so the background cost of the one screen designed to stay open all shift grew with each "Load more" and never came back down. Measured against the deployment at 5,000 deliveries: **29 KB per tick with one page, 610 KB with twenty** — around 800 MB per operator per eight-hour shift. P-6.5 bounded the queue at ten pages so the cost has a ceiling and the cap is announced on screen, but the underlying behaviour is unchanged and the same shape exists in any other infinite query left polling. `[Claude · 2026-08-04]` | Bounded today, so not urgent; but the ceiling is a workaround, and a second long-lived infinite list would reintroduce the growth | Poll only the first page (the queue is newest-first, so that is where new work appears) and leave older pages to the SSE invalidation that already refreshes them. Needs care: TanStack refetches all pages on invalidation too | low |
| TD-55 | deploy / demo | **The demo seeder rides inside a service image, and nothing rebuilds it when the seed changes.** `infra/docker/demo.sh reset` — the one command run before a demonstration — executes `tools/seed/demo.ts` out of `vip/identity:local`, which is rebuilt only when a service is. Found at the P-6.5 freeze: the image predated the milestone's own fix, so the reset restored a fabricated retry count the platform had never had, on a deployment whose source, tests and verification scripts were all correct. `[Claude · 2026-08-05]` | A tool nothing restarts and nothing health-checks can run stale indefinitely, and it is only ever executed immediately before somebody shows the product to a customer | Mitigated now: `docs/review/p6/deployment-integrity.mjs` §5 compares the seed inside the image against the tree, and it runs first in the gate. Pay down properly by giving the seeder its own image built from `tools/seed`, or by having `demo.sh` refuse to run against an image older than the file | medium |
| TD-56 | testing / gate | **The regression that holds the acknowledgement race does not run in `pnpm test`.** `services/notify` excludes `*integration.test.ts` from its default test command, and the suite additionally `describe.skipIf(!online)`s itself when no MongoDB is reachable — so the one test that proves two operators cannot take the same alert is absent from the default gate and silent when it is skipped. The pattern is repo-wide (16 suites, [ED-0022](../docs/project/ENGINEERING_DECISION_LOG.md)); this row is about the **gate**, not the pattern. `[Claude · 2026-08-05]` | A regression guard that is excluded and can silently skip is a guard that can only pass. The race was reintroduced deliberately during the freeze and `pnpm test` stayed green | Mitigated now: the production gate runs `pnpm test:integration` with the dev stack up, and the run is recorded in [VERIFICATION_MATRIX](../docs/review/p6/VERIFICATION_MATRIX.md). Pay down by failing the gate when a suite that carries a named regression skips | medium |
| TD-57 | camera / query | **Health and "not retired" cannot be expressed as a server query.** `CameraQuery` has `zoneIds`, `status`, `lifecycle`, `search`, `limit` and `cursor` — so the console's health filter and its "active (not retired)" default refine the **rows already loaded**, and the page says so. On an estate larger than the loaded pages that is a real narrowing of what an operator can ask for. `[Claude · 2026-08-05]` | An operator on a large estate cannot ask "show me every offline camera" and get an answer about the estate; they get one about the page | Two additive fields (`health`, a negated lifecycle) on `CameraQuery` — a **frozen foundation's** contract, so it needs an implementation issue and an ADR, plus index coverage for the new shapes (L-39) | med |
| TD-58 | camera / console | **Camera names resolve from a bounded page.** `useCameraName` reads one page of up to 200 cameras with a long staleTime and falls back to the id beyond it. The alternative it replaced — fetching the whole estate on every screen that shows a camera id — is what made the console unusable at scale. `[Claude · 2026-08-05]` | Above 200 cameras, an id appears where a name belongs on the incident queue, events and the dashboard (L-40) | A batch resolve on the camera API (`ids=` filter, additive) and a client cache keyed by id | low |
| TD-63 | media / capacity | **The perception tier has no CPU limit and shares a host with recording.** ⚠️ **Escalated from med to HIGH, P-8 Phase 3 (2026-08-05): the speculative case is now measured.** Under the real ONNX backend the runtime took **636–803 % CPU (6–8 cores) at 16 cameras** — the Phase 2 figure with the stub was 3 %. Media held 15–44 % in the same runs, so recording was not starved _on a 10-core host with headroom_; on a smaller host, or with more cameras, nothing in the deployment stops inference from taking the cores the decode path needs, and recording is the contractual obligation. `[Claude · 2026-08-05]` | Inference can starve the decode path that writes evidence; the only thing preventing it today is spare hardware | ⚠️ **The re-measurement is done (P-8 Phase 3H, 2026-08-05) and it sharpens the number.** Clean-host ladder: **797 % CPU (~8 of 10 cores) at 16 cameras**, media 36 %; the drop budget is first exceeded at **8 cameras (8.0 %)**, sustainable is **4 (0.4 %)** — see [AI_RUNTIME_BENCHMARK](../docs/project/AI_RUNTIME_BENCHMARK.md). Remaining work is the limit itself: set `cpus:` on the inference service at or below the point that leaves the decode path its cores, then re-run the ladder to confirm the sustainable count does not fall below 4. `available_cores()` already sizes the thread pool from whatever limit is set | **high** |
| TD-64 | inference / quality | **There are no accuracy gates on the registered model.** Split from TD-5 when the real backend landed. The deployment verification proves the _path_ — 2 detections on a photograph of 2 people, 0 on a test pattern — and proves nothing about _quality_: no labelled corpus, no mAP, no FP/FN promotion gates, no regression check when a model is replaced. ⚠️ The platform can therefore say **inference runs** and must not say **how well it works**; C-14b's pilot column stays ⬜ for exactly this reason. `[Claude · 2026-08-05]` | A future model swap could be worse and nothing would catch it; no basis for any accuracy claim to a customer | Assemble a labelled CCTV corpus (ai/datasets), add an evaluation run to the model-registration path, and gate promotion on FP/FN thresholds recorded in `models/registry.json` | med |
| TD-59 | camera / search | **Camera search is a case-insensitive regex over seven fields.** P-6.6 widened it from the name alone so that moving the search server-side did not remove six fields the browser used to match. It is a linear read of a tenant's cameras: measured flat to 5 000 (12 ms p95), and it will not stay flat for ever. `[Claude · 2026-08-05]` | An estate an order of magnitude larger than anything measured would make the search the slowest query on the screen | A text index (loses substring matching) or a search field maintained on write — decide when a real estate crosses ~50 000 cameras, with the measurement in hand | low |
| ~~TD-60~~ | inference / observability | ✅ **RESOLVED P-8 Phase 2.** The runtime emits structured JSON on stdout with pino field names (`level`/`time`/`msg`), one line per control-plane request, plus a **30 s heartbeat** carrying frames, cameras, sessions, memory and CPU — ⚠️ whose _absence_ is the signal, because an idle runtime and a wedged one produce identical metrics. Frames are deliberately **not** logged per request (32/s would bury everything else). A client disconnect is a warning line, not the Python traceback it used to print. `[Claude · 2026-08-05]` | — | — | — |
| ~~TD-61~~ | inference / capacity | ✅ **RESOLVED P-8 Phase 2.** `available_cores()` reads the cgroup v2 `cpu.max` (then v1) and takes the lesser of quota and `os.cpu_count()`, never below one; the compute label says which number it is. **Proven under a real limit**: with `cpus: 2` the runtime reports `CPU (2 cores, cgroup-limited from 10)` and `capacityUnits 2.0`, where `os.cpu_count()` in the same container still says 10. `[Claude · 2026-08-05]` | — | — | — |
| TD-62 | inference / platform | **The runtime is absent from the System Health page**, so a dead perception tier renders a wholly green system; and its `/ready` is outside the `{success,data}` envelope the ten TypeScript services use. ⚠️ Now that frames actually flow (Phase 2), this matters sooner than it did. `[Claude · 2026-08-05]` | An operator's health page is silent about the component most likely to be saturated | Add the runtime to the gateway's component fan-out and reconcile the readiness envelope — sequenced with Phase 4, when the scheduler makes it load-bearing | med |

## Resolved debt

| ID                                                                                      | Area         | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Commit/PR    | Date       |
| --------------------------------------------------------------------------------------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ---------- |
| **TD-52**                                                                               | tools / seed | **Paid in full, and then some.** Incident ids are now derived from the vertical and the spec's index (`stableUuid('incident:{tenant}:{n}')`, shaped as a valid v4 so the contracts still parse it), so `put`'s upsert matches on a re-run instead of inserting a second complete set. ⚠️ Upserting alone was **not enough**: anything an earlier run left behind under different ids survived every subsequent run, so the seed now **sweeps** — after writing, it removes demo-tenant documents in the collections it owns (`incidents`, `notifications`, `events`) whose ids it did not write, and reports what it removed. Scoped to the demo tenants and to collections it writes; a real tenant is never touched. Verified by running it three times in a row against the deployment: identical counts each time (19 incidents / 32 notifications / 127 events), with the non-demo `tnt_dev` tenant's rows correctly left alone. `[Claude · 2026-08-04]`                                                                                                                 | P-6.5 freeze | 2026-08-04 |
| **TD-44**                                                                               | identity     | **Paid in full — the last pilot blocker.** `UpdateUserInput` frozen (roles only); `SetUserPasswordInput` added; identity gained `GET /users/:id`, `PATCH /users/:id`, `POST /users/:id/{disable,enable,password}`, each gated on `user:update`. ⚠️ **Disabling revokes every refresh-token family**, so it takes effect now rather than whenever the person next signs out — the status flip alone would have passed a test that only re-read the record. Self-disable refused at the service, not just the UI. Console screen at `/users`. Verified against the production deployment: **19/19 API checks** (`docs/review/p6/offboarding.mjs`) and **24/24 browser checks** (`users-ui.mjs`), including a cross-tenant disable returning 404 rather than 403. ⚠️ One defect found only by deployment measurement: `sessionsRevoked` counted token _records_, reporting 3 for a user with 2 sessions, because rotation leaves the used record behind — a session is a **family**. `[Claude · 2026-08-04]`                                                                     | P-6.2        | 2026-08-04 |
| **TD-33 (partly)**                                                                      | console      | **The crash half is paid; the `shortId` half remains open.** A design-system primitive can no longer take a page down on an unexpected enum value: `statusTokens` returns `idle` rather than `undefined`, `StatusIndicator` normalises its `status`, and camera health and lifecycle are read through `healthPresentation` / `cameraLifecyclePresentation`, which render an unmapped value as itself. Found by loading `/cameras` in the production deployment, where one demo camera stored `"degraded"` — not a member of `CameraHealthStatus` — and white-screened the page for three of the four demo tenants. Pinned by `cameraPresentation.status.test.ts`. `[Claude · 2026-08-04]`                                                                                                                                                                                                                                                                                                                                                                                     |
| **TD-25**                                                                               | evidence     | **Paid in full, before the workspace panel that needed it.** `services/evidence/src/adapters/indexes.ts` declares the set as data; `ensureIndexes` builds from it and **reconciles three names whose key set changed** (an `IndexOptionsConflict` would otherwise fail the service at boot — proven by an integration test that recreates the pre-P-5.1 shape and asserts the old index really did sort in memory). Four new indexes for the identity filters. Verified four ways: the coverage model — **strengthened**, because the model ported from Workflow declared the defective index `covered`; it now walks the **whole** compound sort and classifies filters by **cardinality** (an identity filter must be indexed; a two-valued enum may ride the time index) — plus `explain('executionStats')` planner assertions, an **examined/returned** assertion, and a MongoDB benchmark at 10k/100k/500k. Result: every ratio **1:1 and flat**; `eventId` went from **500 examined to return 1** → **1/1**. [ADR-0030](../docs/adr/ADR-0030-incident-prerequisites.md) | P-5.1        | 2026-08-03 |
| **TD-22**                                                                               | workflow     | **Paid in full, before the filters that needed it.** `services/workflow/src/adapters/indexes.ts` declares the index set as data; `ensureIndexes` builds from it; nine new indexes each end in the `(raisedAt, id)` cursor pair. Verified three ways: `test/index-coverage.test.ts` (declaration + sort, always runs), `explain()` planner assertions in `test/mongo-integration.test.ts` (ran for real — ten filters, no `SORT`, no `COLLSCAN`), and `bench/incident-search.bench.ts` at 100/1k/10k/100k. The coverage model is three-valued rather than the two-valued tenant/camera one — see [ADR-0029](../docs/adr/ADR-0029-incident-workflow-entry-criteria.md) §5.                                                                                                                                                                                                                                                                                                                                                                                                      | P-5.0        | 2026-08-03 |
| _(TD-7's per-event query half was paid down in P-4; its Redis half remains open above)_ |              |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |              |            |

## Standing "intentional non-debt" (design choices that look like debt but are not)

These are deliberate, ADR-backed decisions — do **not** "fix" them without an ADR reversal:

- Pooled multi-tenancy by default (siloed only for enterprise) — [ADR-0003](../docs/adr/ADR-0003-tenant-isolation-strategy.md).
- Raw detections are ephemeral (TTL'd), only aggregates/events persist — [18-DATA-ARCHITECTURE](../docs/architecture/18-DATA-ARCHITECTURE.md).
- Smart-clip-only storage (no continuous cloud recording by default) — [12-EVIDENCE-MANAGEMENT](../docs/architecture/12-EVIDENCE-MANAGEMENT.md).
- Eventual consistency between planes (at-least-once + idempotency) — [ADR-0005](../docs/adr/ADR-0005-event-driven-backbone.md).

## Cross-references

[PROGRESS.md](PROGRESS.md) · [TASK-BOARD.md](TASK-BOARD.md) · [../docs/adr/](../docs/adr/)

{
  "name": "@vip/crypto",
  "version": "0.1.0",
  "private": true,
  "description": "At-rest secret vaulting (Phase 1, P1-3): authenticated AES-256-GCM envelope seal/open over a key derived from an `.env` app secret (ADR-0018). Build-free (node:crypto only), pure + testable. See docs/architecture/phase1/CAMERA_ARCHITECTURE.md, 15-SECURITY.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}

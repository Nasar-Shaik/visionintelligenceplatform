node_modules
dist
build
coverage
.turbo
pnpm-lock.yaml
docs/_archive

# ⚠️ Machine-written verification evidence. These are MEASUREMENTS, not source: a verification run
# rewrites them, and prettier's array wrapping does not agree with `JSON.stringify(x, null, 2)`. The
# result was a production gate that went red because a verification had been run — which trains people
# to re-run the gate, and the next real failure gets re-run too. Formatting a measurement changes
# nothing about what it measured.
docs/review/**/*-samples.json
docs/review/**/*-capacity.json
docs/review/**/*-mutations.json
docs/review/**/*-resilience.json
docs/review/p8/tracking-truth.json
docs/review/p9/h265-decode.json
docs/review/p9/baseline/*.json

# ⚠️ Generated by tools/annotator/build_annotator.py, and a test asserts it byte-matches what that
# build writes. Formatting it here would make the checked-in file disagree with its generator — the
# freshness check would go red and the fix would be to un-format it. Edit annotator.template.html.
tools/annotator/pose-annotator.html

# ⚠️ GENERATED, and two gates wanted to own it (P-9). `CAMERA_COMPATIBILITY_MATRIX.md` is emitted by
# `certify_cli.py --matrix --markdown`, and a unit test regenerates it and demands a byte match — the
# guard that stops a support matrix being edited by hand, which is the direction it always drifts.
# Prettier realigns the table columns, so the two gates contradicted each other and the drift guard
# lost. Generated documents are not prettier's to format.
docs/project/CAMERA_COMPATIBILITY_MATRIX.md

# Inference runtime — HEAVY deps for the `onnx` backend only (integration/production).
# The default `stub` backend and the entire unit-test suite are STDLIB-ONLY and need none of these
# (matching ai/mlops). Pinned + registry-verified (docs/project/DEPENDENCIES.md).
#
#   python -m pip install -r ai/inference/requirements.txt   # only to run the real ONNX backend
onnxruntime==1.19.2
numpy==2.0.2
pillow==11.3.0
# ⛔ Live source decoding. `stream_source.py` and `video_decoder.py` import it; it was in neither
# requirements file until P-9 A2 found `POST /streams/validate` crashing on `ModuleNotFoundError` in
# the deployed image. Same pin as requirements-runtime.txt — two files, one set of versions, or the
# deployed runtime is not the one that was tested.
opencv-python-headless==4.10.0.84
# Model Registry client — resolves + downloads model artifacts (aligned with ai/mlops).
mlflow==3.14.0
boto3==1.43.56
# Event backbone client — only for the `nats` event sink (INFERENCE_EVENT_SINK=nats). Pure-Python
# asyncio client; publishes detections onto NATS JetStream (docs/architecture/09, ADR-0016).
nats-py==2.15.0

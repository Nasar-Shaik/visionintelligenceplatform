# MLOps registry integration — pinned, registry-verified (docs/project/DEPENDENCIES.md).
# Client-side only: talks HTTP to the MLflow tracking server and S3 (MinIO) for DVC.
# The server's Postgres driver lives in the MLflow image, not here.
mlflow==3.14.0
dvc==3.67.1
dvc-s3==3.3.0
boto3==1.43.56
